<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Panel</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 min-h-screen flex">

  <!-- Sidebar -->
  <aside class="w-64 bg-indigo-700 text-white flex flex-col p-4">
    <h2 class="text-2xl font-bold mb-6">Admin Panel</h2>
    <nav class="space-y-2">
      <a href="/dashboard" class="block px-3 py-2 rounded hover:bg-indigo-600">Dashboard</a>
      <a href="/applications" class="block px-3 py-2 rounded hover:bg-indigo-600">Applications</a>
      <a href="/club/new/registration" class="block px-3 py-2 rounded hover:bg-indigo-600">New Recruitment</a>
      <a href="/club/all/registrations" class="block px-3 py-2 rounded hover:bg-indigo-600">All Recruitments</a>
      <% if (user && user.role === 'admin') { %>
        <a href="/new/user" class="block px-3 py-2 rounded hover:bg-indigo-600">Create User</a>
      <% } %>
      <% if (user && user.role === 'superadmin') { %>
        <a href="/signup" class="block px-3 py-2 rounded hover:bg-indigo-600">Create User</a>
      <% } %>
      <a href="/logout" class="block px-3 py-2 rounded hover:bg-indigo-600">Logout</a>
    </nav>
  </aside>

  <!-- Content -->
  <main class="flex-grow p-6">
    <%- body %>
  </main>
</body>
</html>
